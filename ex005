#Exercício para anotar o sucessor e o antecessor
n= float (input('Digite um número'))

antecessor= n-1
sucessor= n+1

print ('O antecessor de {} é {} e seu sucessor é {}'.format(n, antecessor, sucessor))
#Podemos colocar isso mais resumido para não utilizarmos 3 variáveis: n, antecessor e o sucessor.
#Assim: 
#n= int (input ('Digite um número'))
#print ('O antecessor de {} é {} e seu sucessor é {}'.format(n, (n - 1), (n + 1)))